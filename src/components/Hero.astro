<section class="hero wrapper">
  <article class="hero-article flow">
    <h1>A Simple Bookmark Manager</h1>
    <p>
      A clean and simple interface to organize your favourite websites. Open a new 
      browser tab and see your sites load instantly. Try it for free.
    </p>
    <div class="get-on-wrapper">
      <button class="button get-on-btn">Get it on Chrome</button>
      <button class="button-secondary get-on-btn">Get it on Firefox</button>
    </div>
  </article>
  <picture class="hero-picture">
    <img src="/illustration-hero.svg" alt="Screen showing bookmark manager">
  </picture>
</section>

<style>
  .hero {
    --accent: var(--s-blue);

    display: flex;
    justify-content: space-between;
    padding-block: 2.8rem;
    margin-bottom: 2rem
  }

  .hero-article {
    padding-top: 4rem;
    max-width: 42ch;
  }

  .hero-article h1 {
    font-size: 2.8rem;
  }

  .get-on-wrapper {
    display: flex;
    gap: 1rem;
  }

  .hero-picture {
    position: relative;
    height: 510px;
    width: fit-content;

    &::before {
      content: '';
      display: block;
      min-width: 100dvh;
      height: 370px;
      position: absolute;
      bottom: 0;
      left: 46%;
      background-color: var(--s-blue);
      border-radius: 150px 0px 0px 150px
    }
  }

  .hero-picture img {
    transform: translateX(95px);
  }

  @media (width <= 768px) {
    .hero {
      flex-direction: column-reverse;
    }

    .hero-article {
      text-align: center;
      margin-inline: auto;
    }

    .hero-picture {
      width: 100%;
      display: flex;
      height: 350px;
      align-items: center;
      justify-content: center;
    }

    .hero-picture::before {
      left: 25%;
      height: 240px;
      bottom: 0%
    }

    .hero-picture img {
      transform: translateX(0px);
      width: min(500px, 100%);
      height: fit-content;
    }

    .hero-article h1 {
      font-size: 2rem
    }

    .get-on-wrapper {
      justify-content: center;
    }
  }

  @media (width <= 480px) {
    .hero {
      padding-top: 2rem
    }

    .hero-picture {
      width: 100%;
      display: flex;
      height: 230px;
      align-items: center;
      justify-content: center;
    }

    .hero-picture::before {
      left: 18%;
      height: 170px;
      bottom: 0%
    }
  }

  @media (width <= 400px) {
    .hero {
      padding-top: 0rem
    }
  }
</style>
