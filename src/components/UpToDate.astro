<section class="up-to-date">
  <div class="up-to-date-wrapper wrapper flow">
    <h3>
      <span class="joined">35.000</span>+
      Already joined
    </h3>
    <h2>Stay up-to-date with what we’re doing</h2>
    <form class="contact-us-form" novalidate>
      <input
        type="email"
        id="input-email"
        size="30"
        required
        placeholder="Enter your email address"
        autocomplete="off"
      />
      <span class="invalid-email-txt">
        Whoops, make sure it's an email
      </span>
      <button type="button" class="button">Contact Us</button>
    </form>
  </div>
</section>

<style>
  .up-to-date {
    --accent: var(--s-red);
    background-color: var(--s-blue);
    padding-block: 4rem;
  }

  .up-to-date-wrapper {
    text-align: center;
    color: var(--white)
  }

  .up-to-date-wrapper h3 {
    text-transform: uppercase;
    font-size: .85rem;
    letter-spacing: 3px;
    font-weight: 400
  }

  .up-to-date-wrapper h2 {
    max-width: 22ch;
    line-height: 1.3;
    font-size: 1.6rem;
    margin-inline: auto;
  }

  .contact-us-form {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    position: relative;
    width: fit-content;
    margin-inline: auto
  }

  .contact-us-form > * {
    height: 100%;
  }

  .contact-us-form input {
    padding-inline: 1.2rem;
    width: 270px;
    height: 45px;
    z-index: 90;

    &::placeholder {
      color: var(--gsh-blue );
      opacity: .6;
    }
  }

  .contact-us-form input:user-invalid ~ span {
    display: flex;
  }

  .invalid-email-txt {
    display: none;
    position: absolute;
    align-items: end;
    justify-content: start;
    left: -2px;
    top: -2px;
    width: 274px;
    min-height: 70px;
    font-size: .6rem;
    font-style: italic;
    padding-inline: .8rem;
    padding-bottom: .2rem;
    background-color: var(--s-red);
		border-radius: 5px;
  }

  @media (width <= 480px) {
    .contact-us-form {
      flex-direction: column;
    }

    .contact-us-form button {
      width: 100%;
    }

    .contact-us-form input:user-invalid {
      margin-bottom: 1.5rem;
    }

    .invalid-email-txt {
      height: fit-content!important;
    }
  }
</style>

<script>
  const contactForm = document.querySelector('.contact-us-form') as HTMLFormElement

  contactForm.addEventListener('submit', (e) => {
    e.preventDefault()
  })
</script>
